const initialState = { 
    options: []
  }
  
  export default function setOption (state = initialState, action) {
    let nextState
    switch (action.type) {
      case 'SET_OPTION':
        return state
      default:
        return state
    }
  }
  